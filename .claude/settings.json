{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; [[ \"$f\" == *.go ]] && make -C /Users/hayeon/alana/shopify-klaviyo-relay check; } 2>/dev/null || true",
            "statusMessage": "make check"
          }
        ]
      }
    ]
  }
}
